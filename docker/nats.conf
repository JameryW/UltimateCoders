# NATS JetStream config for CI / resource-constrained environments.
# Command-line flags in nats:2.10 only support -js and -sd;
# all other JetStream settings must go through a config file.

jetstream {
    store_dir: "/data"
    max_mem_store: 256MB
}
