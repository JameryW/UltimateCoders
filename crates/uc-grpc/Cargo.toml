[package]
name = "uc-grpc"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
uc-types = { path = "../uc-types" }
uc-engine = { path = "../uc-engine", default-features = false }
tokio = { workspace = true }
tonic = { workspace = true }
tonic-health = "0.12"
prost = { workspace = true }
prost-types = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
async-trait = { workspace = true }
async-stream = "0.3"
tokio-stream = "0.1"
futures = { workspace = true }
uuid = { workspace = true }

# NATS (feature-gated)
async-nats = { workspace = true, optional = true }

[features]
default = []
messaging = ["async-nats", "uc-engine/messaging"]

[build-dependencies]
tonic-build = { workspace = true }
# Keep local Windows/macOS builds self-contained; CI/Docker may still provide
# a system protoc, which remains preferred through the PROTOC override.
protoc-bin-vendored = "3"

[dev-dependencies]
tokio-stream = "0.1"
