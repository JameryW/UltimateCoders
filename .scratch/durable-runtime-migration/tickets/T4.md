Status: ready-for-agent
Blocked by: T3
Design refs: D4 (#633), D7 (#636), §3 G4

## What to build

at-least-once + 幂等闭环：
1. 发布侧设 `Nats-Msg-Id` = idempotency_key（激活 JetStream duplicate_window），删除内存 TTL 去重作为唯一保障。
2. worker 端 attempt 级去重：该 attempt 已有结果 → ack no-op；替换 best-effort checkpoint-skip（worker.py:975）为 attempt 范围判定。
3. 迟到结果按 epoch/attempt 在 gateway 拒绝 + 计数与事件。
4. 旧信封滞留消息：term 丢弃 + `stale_dispatch_dropped` 计数，gateway 健康/Dashboard 可见（D7，无 DLQ）。
5. 删除 T1 过渡期旧字段双写。

## Acceptance criteria

- [ ] 真 JetStream 集成测试：redelivery / 重复 publish / acked-after-exec 三路去重场景。
- [ ] 迟到旧 attempt 被拒且留可观测痕迹。
- [ ] 旧信封消息不执行、计数可见。
