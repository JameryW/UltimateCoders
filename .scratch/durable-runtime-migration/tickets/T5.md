Status: needs-info（等 T3 落地后把 seam 细节定稿）
Blocked by: T3, D4 #633（已决）
Design refs: D4 (#633) 全部三点, §3 G5

## What to build

统一 Executor trait（uc-engine）：`execute(envelope) -> AttemptOutcome`；实现 LocalExecutor / NatsExecutor / SandboxExecutor / RemoteExecutor。
1. **JetStream 硬依赖**：删 worker Core 静默降级（nats_worker.py L486–503）；JS 不可用 → 拒绝注册 + 周期重试 + 健康/Dashboard 可见；UC_SUBTASKS 流创建归 gateway 侧供给。
2. **effect_class 白名单**：仅 local_safe/read_only 节点可在 NatsExecutor 故障时走 LocalExecutor；Coding 节点保持 READY 排队 + 告警。
3. 消灭分解分叉：删 `decompose_task` 本地换行切分（task_store.rs L566–673）——NATS 可用性不得改变 WHAT（D2 已定分解权威归属 TS planner；本票只删 Rust 侧回退分叉）。
4. 消灭 no-op `DispatchMode::Local`；ScheduleDispatcher 吸收或显式废弃。

## Acceptance criteria

- [ ] NATS-down 集成测试：local_safe 图继续推进、coding 节点停在 READY + 告警、WHAT 字节级不变。
- [ ] JS 不可用 worker 拒注册且状态可观测（测试替身必须镜像真实 JS 客户端行为，防假通过）。
- [ ] 无头 `grep` 验证：Core 降级路径与 decompose 分叉代码已不存在。
