Status: ready-for-agent
Blocked by: T3, T4, D6 #635（已决）, D7 #636（已决）
Design refs: D2 一刀切反转, §3 G2/G8

## What to build

权威一刀切反转 + TS wave 机器拆除：
1. TS 全部状态转移经 Rust API 并以其结果为准；删 `resyncAllTasksToGrpc` 反向 push（orchestrator.ts L319/2103）；`.uc/tasks` 只读化为 UI 缓存。
2. 删 TS wave 循环与 wave 快照：`splitWavesByFileOverlap`/`executeWaves`/`resumeFromWave`/`.uc/checkpoints`（FileIntentTracker、CircuitBreaker 按图语义重植或显式退役并记录）。
3. 按 D6 落地：软暂停 + `UC_PAUSE_GRACE_SECS`（默认 120s）超时升级 node 级 cancel（依赖 T7 原语，可与 T7 同车交付）+ node 态重算 resume。
4. 文件重叠禁并行 → `conflict_risk` 分级（same file/class/symbol 阈值 0.4/0.8；为 P1 affinity 调度留接口，不过度设计）。
5. 发布说明按 D7 四要点（lockstep 顺序、未 commit 重跑语义、stale-drop 计数位置、回滚=旧镜像+重导入）。
6. 处置暴露出的 TS review 死代码（`reviewSubtask` 未接线）：删除或在票面记录去向。

## Acceptance criteria

- [ ] 崩溃注入恢复测试：重启后无重复执行已 commit node、无丢失 READY 节点。
- [ ] 菱形图 TS-free 端到端：经 Rust API 暂停→单分支 commit→宽限硬停→resume 重派。
- [ ] 全仓 `grep` 证明 wave 机器已除（resumeFromWave/checkpoints/splitWavesByFileOverlap 为零）。
- [ ] `.uc/tasks` 变为缓存证据：删除文件后 UI 可由 Rust 重建。
