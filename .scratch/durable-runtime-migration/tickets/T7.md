Status: ready-for-agent
Blocked by: T6
Design refs: §3 G6, D6 (#635) 第 2/3 点

## What to build

取消下沉到 node/attempt 级：
1. `CancelTaskRequest` 扩展 subtask/node + attempt 粒度（proto + 桩重生成）；gateway 处理并沿 `uc.task.event` 控制面发 node/attempt 级 cancel（D4：P0 控制通道维持 core pub/sub）。
2. worker 响应：杀 attempt 进程组，结果按 fenced 上报。
3. **cancel-attempt-keep-node**（D6）：被取消 attempt 的 node fence 后回 READY，不终态化 FAILED——pause 宽限超时是内部首个调用方。
4. 删 TS `cascadeCancel`/`reverseCascadeUnCancel`；pause grace 与 Dashboard 控制改走新动词。

## Acceptance criteria

- [ ] RUNNING attempt 取消 → node 回 READY → 重派成功 commit 端到端测试。
- [ ] 被取消 attempt 的迟到结果被 fencing 拒绝。
- [ ] node 级取消不误伤兄弟节点。
- [ ] 级联取消 = 运行时依赖闭包计算，非 TS 簿记（删净 cascade 代码）。
