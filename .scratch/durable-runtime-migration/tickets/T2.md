Status: ready-for-agent
Blocked by: T1
Design refs: assessment D1（状态表权威+事件审计）, §3 G1

## What to build

图状态行表 + 一次性导入：
1. `execution_graphs / graph_nodes / task_attempts` 迁移（idempotent，advisory-lock 模式同 #631；sqlx 走既有 `storage` feature）。
2. 一次性回填：PG 旧 `tasks` JSONB → 图表；`.uc/tasks/*.json` 仅在 PG 无对应 graph 时导入（D2 约束 1，导入即打标，不双向）。
3. gateway 读路径切到行表（feature-gated 影子读），行为不变——权威反转在 T6。

## Acceptance criteria

- [ ] schema 反映信封字段与 effect_class（为 T5/T3 预留）；`version` 列就位。
- [ ] 每个 legacy task/subtask/在飞 attempt 有确定的导入映射；重复运行导入幂等。
- [ ] 真 PG 双后端一致性测试（127.0.0.1 连接串；SQL 排序与内存投影排序不得二义——跨后端测试把关）。
- [ ] 冷启动并发迁移回归（复用 #631 advisory-lock 测试模式）。
