{"file": ".trellis/spec/backend/nats-bridge-spec.md", "reason": "NATS KV lease conventions — the distributed lock uses JetStream KV (jetstream().create_key_value), TTL-based lease, acquire-or-skip semantics."}
{"file": ".trellis/spec/backend/scheduler-spec.md", "reason": "SchedulerService cron callback (register_cron_with_scheduler) — the lock acquisition point before dispatch_with_guard."}
