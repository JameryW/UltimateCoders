{"file": ".trellis/spec/backend/scheduler-spec.md", "reason": "Core executable contract — Rust SchedulerService signatures, NightWindow, ScheduleDispatcher trait, Python Scheduler API, ScheduleStore. The ADR (Rust owns scheduling, dispatch via engine.submit_task) must stay consistent with this spec's OrchestratorDispatcher intent."}
{"file": ".trellis/spec/backend/omp-rpc-server-spec.md", "reason": "gRPC server wiring conventions — how DashboardService delegates to the engine. PR3 routes scheduler RPCs to the engine (not NATS); follow the existing delegation pattern."}
{"file": ".trellis/spec/backend/nats-bridge-spec.md", "reason": "NATS routing context — PR3 REMOVES the NATS round-trip for scheduler RPCs. Need to know what's being replaced and what stays NATS-routed."}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "EngineError conventions for the cron-fire failure path (PR2 records Failed ExecutionHistory on submit_task Err) and the AddCronJob UNIMPLEMENTED stub."}
