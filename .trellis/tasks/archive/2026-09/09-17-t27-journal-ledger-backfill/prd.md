# T27 — #674：journal 账本整备（骨架占位符 → 据实回填；重复残块清除）

## 元信息

- 票号：T27（**#674**）
- 日期：2026-09-17
- 前置：T26（#675 切片 C，`610de67`）
- 交付面：`.trellis/workspace/Jamery Wang/journal-1.md` —— **唯一被改的实现文件**
- 不交付：任何测试/产品代码；不重跑历史门禁（当时环境不可复现）；不改 `journal-2.md` 的任何 session 正文

## 背景

#674 由 T22 收口时派生，票面列了三件事：

1. 14 个 session 的 `### Testing` 只有脚本骨架（`- [OK] (Add test results)`）；
2. Session 13 有一段重复残块（590–601）；
3. 逐条过一遍 `- None - task complete` 是否仍属实。

**票面明确要求「先核实、再据实回填 —— 不臆造」**，所以本票的第一步是**自己量形状**，
而不是照票面的行号与清单开工。

## 侦察结论（全部一手，逐条可复算）

### 结论 A：票面列的 3 类之外，实测**多出 2 类**（同源）

| # | 缺陷类 | 票面 | 实测 |
|---|---|---|---|
| 1 | `### Testing` 只有骨架占位符 | 14 个 session（1–11 / 19 / 20 / 21） | **14 个，清单逐项吻合** ✅ |
| 2 | Session 13 重复残块 | 590–601 | **吻合**（590–601 是被取代的 stub）✅ |
| 3 | `- None - task complete` 待复核 | 「另有一批（12 / 14 / 15 / 22）」 | **19 个 session**（1–15 / 19 / 20 / 21 / 22）—— 票面**不全** |
| 4 | **骨架尾巴残留**（重复 `### Testing`/`### Status` 占位对） | 未提 | **4 处**（Session 12 / 13 / 14 / 15） |
| 5 | `### Main Changes` 仍是 `(Add details)` | 未提 | **6 个 session**（1 / 2 / 3 / 9 / 10 / 11） |
| 6 | 空标题重复（`### Main Changes` ×2、`### Summary` ×2） | 未提 | **4 个 session**（12 / 14 / 15 / 20） |

⇒ **票面的「14 + 1 处残块」低估了工作面：真正要动的是 39 行占位符 + 5 处重复残块。**

### 结论 B：第 2/4/6 类是**同一个机制**，不是三次独立事故

脚本骨架被追加后，作者是**在骨架之上手写正文**的：正文通常从自己的 `### Summary` /
`### Main Changes` **重新起头**，于是——

- 骨架里那句 `(Add details)` 被换成了「一份自带 `### Summary` + `### Main Changes` 的完整文档」
  ⇒ 同名标题出现两次（第 6 类）；
- 正文写完后，**骨架的尾巴**（`### Git Commits` + 占位 `### Testing` + 占位 `### Status` +
  `### Next Steps`）**原样留在下面** ⇒ 占位符与真内容并存（第 4 类）。
  注意：尾巴里的 `### Git Commits` 哈希表是**真的**，必须保留；
- Session 13 的 stub 则是同一机制的极端：**整个骨架块被写了一遍又被取代**（第 2 类）。

⇒ 修法必须**分类**：空标题删掉、占位尾巴删掉、**真内容一律不删**。

### 结论 C：Session 13 的 stub 删掉**不丢信息**

stub 的 Summary（598）里的门禁数字是 **Rust 237 / pytest 1106+5**，
真块（666 / 669）里**同样写着** 237 passed 与 1106 passed / 5 skipped ⇒ stub 是其**子集**。

### 结论 D：回填的证据**就在账本自己里面**

14 个 session 的正文（`### Summary`、`## Gates`、`## 门禁终值`、`## 质量门禁`、`## 测试`、`## 门禁`）
**大多已经有完整数字**——作者把门禁写进了 Summary 或手写的 `##` 段，只是没填 `### Testing`。
⇒ 回填是**有据可依的**，且每一条都标注了来源（`源 = 本段 …`）。
6 个 `(Add details)` 的 session 正文里**确实没有明细**（只有 Summary）⇒ 不回填内容，只给
**归档任务目录**的指针，并明写「当时未写入账本」。

### 结论 E：19 个 `- None - task complete` 都能给出**可核验**的后续

判据用「**下一个 session 的标题所指票据**」+「本段正文里明写的 `## Next` / `## 状态` 陈述」。
两条都不是发明：前者是本账本的下一节，后者是原文。

### 结论 F：`### Git Commits` 缺席（Session 16 / 17 / 18）**本票不动**

这三个 session 段内没有 `### Git Commits`（T25 的收口记录里已经量到）。
本票**故意不补**：17 的正文只出现过 `0c2604d`（CI 验收提交）、18 是 `dd3d2b3`，
而 16 的正文一个自己的哈希都没有 ⇒ 从散文里拼一张「Git Commits」表等于**猜测哪些提交属于该 session**。
⇒ 记账为**已知形态**，另票处理。

## 交付

对 `journal-1.md` 的一次**单遍、可审计**的手术（脚本内每个删除都断言了它要删的**逐字节内容**，
全部断言通过才写盘；写盘后回读比对）：

- **删除 54 行**：Session 13 stub（590–601，12 行）+ 4 处占位尾巴（各 8 行）+ 4 处空标题（1–4 行）
- **替换 39 行**：14 处 `### Testing` 据实回填 + 6 处 `(Add details)` 指针 + 19 处 `### Next Steps`
- 结果：`148,099 → 157,354` 字节、`2000 → 1950` 行、三类占位符**全部归零**
  （`- [OK] (Add test results)` `18 → 0`、`(Add details)` `6 → 0`、`- None - task complete` `19 → 0`）

## 验收

| # | 判据 | 结果 |
|---|---|---|
| 1 | 三类占位符计数归零 | ✅ 0 / 0 / 0 |
| 2 | 每个 session 的 6 个标准标题**各恰好一次** | ✅ 26/26（唯一例外是 16/17/18 本就缺 `### Git Commits`，见结论 F） |
| 3 | `## Session N` 无重复 | ✅ 26 个唯一编号（手术前 Session 13 出现两次） |
| 4 | 每个 session 的 `### Testing` 非占位（≥116 字符） | ✅ 最小 116c |
| 5 | 行尾仍是**统一 CRLF**、**零孤立 LF** | ✅ 1950 CR / 1950 LF / lone-LF 0 |
| 6 | 是**受控改动**而非整文件重写 | ✅ `numstat` = **43 / 93** |
| 7 | 既有门禁不受影响 | ✅ 守卫 `--audit` 仍 `89 ok / 1 stale / 8 ambiguous / 0 structural`、exit 0；`test_check_spec_refs.py` **11 passed** |

## 非目标 / 记账

- 不改任何测试或产品代码；不重跑历史门禁。
- 不给 Session 16/17/18 补 `### Git Commits`（结论 F）。
- 本票只动 `journal-1.md`；`journal-2.md` 的 session 27–29 已是完整段落（0 占位符）。
