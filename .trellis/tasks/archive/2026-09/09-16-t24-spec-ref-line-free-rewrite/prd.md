# T24 #673 — 切片 B：把 50 处「符号优先」引用从 `path:line` 改成 `path`

**Status**: in progress
**Implements**: #673 的**切片 B**（重写本体）
**Depends on**: #675 切片 A（T23，`848b1c7` + `b430f20`）—— 它先把「无行号提及」补进守卫视野；否则本票的重写会让引用**静默移出视野**
**Related**: #672 / #673（T21 / T22）、#675 切片 B/C（47 处悬空提及的处置）、#674（journal 账本）
**Scope**: 只改 `.trellis/spec/**` 的引用形态 + 守卫两处**已测量**的小缺陷；**不**接 CI（那是 #673 切片 C）

## 目标

T21 定的口径是**符号优先**：`Worker._execute_steps`（`worker.py:1053`）→ 去掉行号。
本票把**可机械判定为安全**的那批真的改掉，并把「安全」升级为可判定条件：
**存在性**（有一个锚）不够，还要**唯一性**（剩下的锚能唯一定位到一处）。

## 侦察结论（可复算，命令见 `research/notes.md` §G）

**A. 可去行号 = 51，不是 T23 记录的 52。**

| 分类（优先级阶梯） | 条数 |
|---|---|
| 符号锚 | **42** |
| 唯一内容锚（`content` 字面量在目标文件出现恰好 1 次） | **9** |
| 内容锚歧义（出现 >1 次 ⇒ 去掉行号就分不清指哪一处） | 32 |
| 无锚 | 65 |
| 合计 | 148 |

⚠️ 与 T23 的 52 差 1，**原因正是本票自己改的守卫**：T23 记录里那 1 条是 `error-handling.md:307`，
它当时的唯一锚是散文里的 `delete`（来自 `**Mapping \`MemoryWriteError\` for delete operations**`）。
把 bold 收紧成 **identifier-exact** 后该行只剩「路径 span（按设计被排除）+ 无下划线的 `MemoryWriteError`」
⇒ 该行**确实零锚** ⇒ 不再具备去行号的条件。**这是修正，不是损失** —— 若照旧重写，它剩下的证据是零。

**B. 重写的目标形态，正好是 T23 才补进视野的那一类。** 不先做 T23，本票会让
`148 refs` → `98 refs` 而**没有任何提示**（`REF_RE` 要求 `:(\d+)`）。

**C. 干跑不能靠影子树。** 守卫用**真仓库**解析（`_repo_index()` 走 `ROOT`、`collect()` 里
`spec.relative_to(ROOT)`）：把 `ROOT` 指向仓外影子副本 ⇒ 实测 216/221 目标解析不到；
放在仓内 ⇒ 它自己的 `.md` 会进索引。⇒ 本票的「干跑」只预测**形态**，
真正的证明在**写盘之后**跑真守卫，任一断言失败就按字节回滚。

## 变更

**`scripts/check-spec-refs.py`（两处，都是已测量的小缺陷）**

1. `_symbols_on` 的 bold 分支收紧为 **identifier-exact**（`**Task**` 算符号；多词散文、纯行内代码不算）。
   全仓**恰好改变 1 行**（`error-handling.md:307`：`STALE` → `OK`，`delete` → 无符号），
   删掉的是一条**假**锚。标题仍保持宽松 —— `### Task Properties` 是 T22 找回的 13 条之一。
2. `EXCLUDE_DIRS` 增加 `.scratch`。**判定来源必须可复现**：`os.walk` 不看 git，`.scratch/**`
   是未跟踪的本地状态。实测：一个回滚副本就能让唯一提及变歧义，
   `.scratch/pt-test_*/**/lib.rs` 把 `lib.rs` 的候选数从 **4** 抬到 **79**。
   全仓**恰好改变 1 条判定**（`event-pipeline-spec.md:153` `dashboard/app.py`：ambiguous → resolved），
   **0 条 ref 判定**。

**`.trellis/spec/**`（9 个文件，50 处）**：`` `path:line` `` → `` `path` ``。
**只删** `:行号`，不动行尾、不动正文、不重排。

## 验收

1. 重写 **50** 处（51 条合格 − **1** 条因「重写会离开守卫视野」而扣下：
   `error-handling.md:320` 的 `sandbox.py:1007/1546` 是全文**唯一裸写**（无反引号）**且**唯一 `/双行号` 形态）。
2. **零覆盖损失**（**逐条**断言，不是抽样）：每条重写后的引用都以**提及**被守卫看见（`MENTION_OK`）、
   解析到**同一个**目标文件、并**保留**原有 verdict。
3. 存活引用 **98** 条**逐条 verdict 不变**；守卫 **exit 0**、**0 structural failure**。
4. 改动粒度：`git diff --numstat` = **49/49** —— 50 处落在 **49 行**上
   （`agent-capability-spec.md:603` 一行两引用）。
5. 消融：`.scratch` 子句按 **T0**（提交态）/ **T1**（一处突变）/ **T2**（按字节恢复、校 sha256）三跑；
   T1 必须 ≠ T0、T2 必须 == T0、**三次 exit 全为 0**（不松动门禁）。
6. 每个数字可被**独立复算**（`.scratch/t24-*.py` 与守卫两路）。

## 非目标

- **不接 CI**（#673 切片 C：接线会改变触发面 —— `.trellis/**` 一改就跑 Python CI）。
- **不处理 47 处悬空提及**（#675 切片 B/C）。
- **不做锚点门禁**（提及类只判解析）。
- **不改** 32 条内容锚歧义与 65 条无锚的引用 —— 它们需要补锚或基线兜底，不能机械去行号。
