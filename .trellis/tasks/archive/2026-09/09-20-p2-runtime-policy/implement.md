# Execution plan

1. Pin pure report semantics with a failing worked example, implement metrics.
2. Add read-only PostgreSQL aggregation and JSON CLI; compile storage path.
3. Pin explicit review behavior at sandbox boundary, implement strict verdict
   parsing and prompt injection, run related worker/serialization tests.
4. Pin capacity placement and registry gates; wire validated startup setting.
5. Document the baseline and its limits; run focused suites, formatter/linter,
   relevant feature builds and independent standards/spec reviews.
6. Leave a reviewable diff. Do not claim live PostgreSQL verification without
   an actual database run; do not commit or publish without authorization.
