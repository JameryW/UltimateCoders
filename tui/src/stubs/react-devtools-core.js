/**
 * Empty stub for react-devtools-core.
 * This module is only needed when DEV=true, which we don't use in production.
 * esbuild aliases react-devtools-core to this file to avoid the runtime import error.
 */
export default {
  connectToDevTools: () => {},
};
